# Encrypted credentials (personal, master-password protected)
config.enc

# Plaintext config (deleted after first run, but just in case)
config.json

# Legacy: viewer was previously generated on disk (now served in-memory by the server)
nerdcam.html

# Snapshots
snapshot_*.jpg

# Recordings
recordings/

# Log file
nerdcam.log

# Python
__pycache__/
*.pyc
*.pyo
